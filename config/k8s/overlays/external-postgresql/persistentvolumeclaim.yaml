apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: carepulse-data
  labels:
    app.kubernetes.io/name: carepulse
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

